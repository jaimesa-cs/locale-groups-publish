/** @type {import('next').NextConfig} */
const nextConfig = {
  // transpilePackages: ["@contentstack/venus-components"],
  output: "standalone",
  reactStrictMode: true,
};

module.exports = nextConfig;
